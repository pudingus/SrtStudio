<UserControl x:Class="SrtStudio.Timeline"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SrtStudio"
             mc:Ignorable="d" Height="132" Width="1100" MouseMove="UserControl_MouseMove" PreviewMouseLeftButtonDown="UserControl_PreviewMouseLeftButtonDown" PreviewMouseLeftButtonUp="UserControl_PreviewMouseLeftButtonUp" PreviewMouseMove="UserControl_PreviewMouseMove" >
    <Grid>
        <StackPanel VerticalAlignment="Bottom">
            <Grid>
                <Grid HorizontalAlignment="Left" Height="132" VerticalAlignment="Top" Width="100" >
                    <Rectangle Fill="#FF171717" Stroke="Black" />
                    <TextBlock x:Name="TrackName" Text="Track 1 - eng.srt" Margin="10,10,0,0" Foreground="White"/>
                </Grid>

                <ScrollViewer HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Disabled" CanContentScroll="True" Margin="100,0,0,0">
                    <WrapPanel x:Name="wrap1">
                        <Grid x:Name="grid1" Height="100" Width="500"  >
                            <Rectangle x:Name="rect1" Fill="#FFFF7979" Stroke="#FF320000" />
                            <TextBlock x:Name="SubText" HorizontalAlignment="Left" Margin="10,10,0,0" TextWrapping="Wrap" Text="- Snídaně za pět, zlato." VerticalAlignment="Top"/>
                        </Grid>
                    </WrapPanel>
                </ScrollViewer>
            </Grid>

        </StackPanel>

    </Grid>
</UserControl>
